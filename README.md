# Obsidian Randomizer

Select a file from your Obsidian vault (technically any Markdown file),
then this finds all lists and can give you a random element from them.

Built with [iced](https://iced.rs/).

## Notes

- **Should** handle multi line elements, but that is barely tested.
- Chooses randomly from all elements of a list, regardless of indent and nesting.